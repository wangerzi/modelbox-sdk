let API_TOKEN = '';

function setToken(token) {
  API_TOKEN = token;
}

function uploadModel(blob, config={}) {
  return {};
}

function previewModel(modelId, dom) {
}

export default {
  setToken,
  uploadModel,
  previewModel,
};
